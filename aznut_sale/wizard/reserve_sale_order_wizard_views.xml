<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="reserve_sale_order_wizard_form" model="ir.ui.view">
        <field name="name">aznut_sale.reserve.sale.order.wizard.form</field>
        <field name="model">reserve.sale.order.wizard</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="order_id" domain="[('id','in',allowed_sale_orders_ids)]"
                               options="{'no_create_edit':True,'no_create':True}"/>
                        <field name="quantity"/>
                        <field name="allowed_sale_orders_ids" invisible="1"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_confirm" string="Confirm" type="object" class="btn-primary"/>
                    <button special="cancel" data-hotkey="z" string='Cancel' class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
